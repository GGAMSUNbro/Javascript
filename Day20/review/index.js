// 비밀번호 input & 비밀번호 확인 input 두개 만들고
// 두개가 같아야 밑에 일치버튼이 파란색으로 바뀌는 코드만들기
// 같지않으면 불일치 빨간색

// const pwInput = document.createElement("input");
// const cpwInput = document.createElement("input");
// const checkBtn = document.createElement("button");
// checkBtn.innerText = "불일치";
// checkBtn.style.backgroundColor = "red";

// pwInput.addEventListener('input',(e)=>{
//     const {value} = e.target
//     pwInput.value = value ? checkBtn.innerText ="일치" && checkBtn.style.backgroundColor = 'green':checkBtn.innerText ="불일치" &&checkBtn.style.backgroundColor = 'red'
// })

// cpwInput.addEventListener('input',(e)=>{
//     const {value} = e.target
//     pwInput.value = value ? checkBtn.innerText ="일치" && checkBtn.style.backgroundColor = 'green':checkBtn.innerText ="불일치" &&checkBtn.style.backgroundColor = 'red'
// })
// document.body.appendChild(pwInput);
// document.body.appendChild(cpwInput);
// document.body.appendChild(checkBtn);
